# --- Segredos e config local — NUNCA versionar ---
files/.env
Helpers/destinatarios.py

# --- Banco de dados e backups (regeneráveis; grandes) ---
files/Database/
files/Parquet/
*.db
*.db-journal
*.db-wal

# --- Saídas geradas / dados brutos (regeneráveis) ---
# O log de cada script cai na pasta do próprio script (ver Helpers/logger.py).
codigos/*/logs/
files/logs/
files/backups/
files/relatorios/
files/anbima_data_raw/
files/api_samples/
files/debug/
files/diagnosticos/
files/emails/
files/divergencias_fluxo.csv
files/pu_divergencias.csv
files/validar_calc_b3.csv
files/cobertura_fontes.csv
files/fianalytics_resposta.json
files/*.bak-*
files/*.csv.bak

# --- Python ---
__pycache__/
*.pyc
.venv/
venv/

# --- Playwright ---
.playwright/

# Base temporaria do conferir_dados. Ela ja foi commitada uma vez por engano (895fd88);
# desde 02/09/2026 o script usa a pasta temporaria do sistema, mas a entrada fica como
# rede de seguranca para quem passar um caminho relativo no argumento posicional.
tmp_parquet/
