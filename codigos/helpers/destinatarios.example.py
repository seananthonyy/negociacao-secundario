# Destinatários de email do projeto.
#
# Copie este arquivo para `destinatarios.py` e preencha. O `destinatarios.py`
# NÃO vai para o GitHub (está no .gitignore) — nunca coloque emails reais aqui
# no `.example`.

# Rascunho do relatório diário (gerar_relatorio_credito.py --email-dia).
EMAIL_DESTINATARIOS = ["preencher@exemplo.com", "outro@exemplo.com"]

# Emails de status [OK]/[ERROR] dos scripts.
# Deixe [] para usar a variável de ambiente OUTLOOK_TO; ou liste emails aqui.
OUTLOOK_TO = []
